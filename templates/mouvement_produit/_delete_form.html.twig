<form method="post" action="{{ path('app_mouvement_produit_delete', {'id': mouvement_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mouvement_produit.id) }}">
    <button class="btn">Delete</button>
</form>
