{% extends 'base.html.twig' %}

{% block title %}MouvementProduit{% endblock %}

{% block body %}
    <h1>MouvementProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mouvement_produit.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ mouvement_produit.quantite }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ mouvement_produit.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ mouvement_produit.date ? mouvement_produit.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>PrixUnitaire</th>
                <td>{{ mouvement_produit.prixUnitaire }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ mouvement_produit.motif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mouvement_produit_index') }}">back to list</a>

    <a href="{{ path('app_mouvement_produit_edit', {'id': mouvement_produit.id}) }}">edit</a>

    {{ include('mouvement_produit/_delete_form.html.twig') }}
{% endblock %}
